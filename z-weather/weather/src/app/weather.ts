export class Weather {
    constructor(public temp: number, public feelsLike: number, public humidity: number, public tempHigh: number,
        public tempMin: number, public city: string, public country: string, public weatherOverview: string, public weatherDescription: string,
        public windSpeed: number, public windDirection: number, public weatherIcon : string) {
        // this.temp =  temp, this.feelsLike = feelsLike, this.humidity = humidity, this.tempHigh = tempHigh, this.tempMin = tempMin, this.city = city, 
        // this.country = country, this.weatherOverview = weatherOverview, this.weatherDescription = weatherDescription, this.windSpeed = windSpeed,
        //  this.windDirection = windDirection;





    }
}